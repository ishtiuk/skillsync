
import React from 'react';
import { TalentHubAuth } from '@/components/auth/TalentHubAuth';

const Register: React.FC = () => {
  return <TalentHubAuth initialMode="signup" />;
};

export default Register;
