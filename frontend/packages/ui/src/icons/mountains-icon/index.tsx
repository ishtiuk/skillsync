import { type IconProps, Mountains } from '@phosphor-icons/react';

function MountainsIcon({ size, color }: IconProps) {
  return <Mountains weight="fill" size={size} color={color} />;
}

export { MountainsIcon };
