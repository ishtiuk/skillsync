import { type IconProps, Basketball } from '@phosphor-icons/react';

function SportsIcon({ size, color }: IconProps) {
  return <Basketball weight="fill" size={size} color={color} />;
}

export { SportsIcon };
