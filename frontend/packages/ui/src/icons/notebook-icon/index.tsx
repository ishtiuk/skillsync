import { type IconProps, Notebook } from '@phosphor-icons/react';

function NotebookIcon({ size, color }: IconProps) {
  return <Notebook weight="fill" size={size} color={color} />;
}

export { NotebookIcon };
