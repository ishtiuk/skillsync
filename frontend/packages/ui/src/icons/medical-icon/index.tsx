import { type IconProps, FirstAidKit } from '@phosphor-icons/react';

function MedicalIcon({ size, color }: IconProps) {
  return <FirstAidKit weight="fill" size={size} color={color} />;
}

export { MedicalIcon };
