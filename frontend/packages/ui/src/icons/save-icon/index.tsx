import { type IconProps, BookmarkSimple } from '@phosphor-icons/react';

function BookmarkIcon({ size, color }: IconProps) {
  return <BookmarkSimple weight="fill" size={size} color={color} />;
}

export { BookmarkIcon };
