import { type IconProps, CheckCircle } from '@phosphor-icons/react';

function CheckCircleIcon({ size, color }: IconProps) {
  return <CheckCircle weight="fill" size={size} color={color} />;
}

export { CheckCircleIcon };
