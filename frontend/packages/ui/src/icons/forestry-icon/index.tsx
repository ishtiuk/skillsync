import { type IconProps, Tree } from '@phosphor-icons/react';

function ForestryIcon({ size, color }: IconProps) {
  return <Tree weight="fill" size={size} color={color} />;
}

export { ForestryIcon };
