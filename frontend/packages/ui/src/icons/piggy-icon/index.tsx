import { type IconProps, PiggyBank } from '@phosphor-icons/react';

function PiggyBankIcon({ size, color }: IconProps) {
  return <PiggyBank weight="fill" size={size} color={color} />;
}

export { PiggyBankIcon };
