import { type IconProps, UsersThree } from '@phosphor-icons/react';

function UsersIcon({ size, color }: IconProps) {
  return <UsersThree weight="fill" size={size} color={color} />;
}

export { UsersIcon };
