import { type IconProps, Warehouse } from '@phosphor-icons/react';

function WarehouseIcon({ size, color }: IconProps) {
  return <Warehouse weight="fill" size={size} color={color} />;
}

export { WarehouseIcon };
