import { type IconProps, GraduationCap } from '@phosphor-icons/react';

function GraduationCapIcon({ size, color }: IconProps) {
  return <GraduationCap weight="fill" size={size} color={color} />;
}

export { GraduationCapIcon };
