import { type IconProps, Folder } from '@phosphor-icons/react';

function FolderIcon({ size, color }: IconProps) {
  return <Folder weight="fill" size={size} color={color} />;
}

export { FolderIcon };
