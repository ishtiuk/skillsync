import { type IconProps, Television } from '@phosphor-icons/react';

function MediaIcon({ size, color }: IconProps) {
  return <Television weight="fill" size={size} color={color} />;
}

export { MediaIcon };
