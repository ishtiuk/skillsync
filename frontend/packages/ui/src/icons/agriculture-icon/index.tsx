import { type IconProps, Carrot } from '@phosphor-icons/react';
function AgricultureIcon({ size, color }: IconProps) {
  return <Carrot weight="fill" size={size} color={color} />;
}

export { AgricultureIcon };
