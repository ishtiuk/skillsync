import { type IconProps, Barricade } from '@phosphor-icons/react';

function BarricadeIcon({ size, color }: IconProps) {
  return <Barricade weight="fill" size={size} color={color} />;
}

export { BarricadeIcon };
