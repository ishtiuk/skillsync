import { type IconProps, Lightning } from '@phosphor-icons/react';

function EnergyIcon({ size, color }: IconProps) {
  return <Lightning weight="fill" size={size} color={color} />;
}

export { EnergyIcon };
