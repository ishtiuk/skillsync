import { type IconProps, Bridge } from '@phosphor-icons/react';

function BridgeIcon({ size, color }: IconProps) {
  return <Bridge weight="fill" size={size} color={color} />;
}

export { BridgeIcon };
