import { type IconProps, WarningDiamond } from '@phosphor-icons/react';

function WarningDiamondIcon({ size, color }: IconProps) {
  return <WarningDiamond weight="fill" size={size} color={color} />;
}

export { WarningDiamondIcon };
