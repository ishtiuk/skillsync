import { type IconProps, Circuitry } from '@phosphor-icons/react';

function CircuitryIcon({ size, color }: IconProps) {
  return <Circuitry weight="fill" size={size} color={color} />;
}

export { CircuitryIcon };
